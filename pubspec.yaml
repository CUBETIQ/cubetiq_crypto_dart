name: cubetiq_crypto_dart
description: A basic cryptojs for cubetiq application data encryption and protection.
version: 1.0.0

environment:
  sdk: ">=2.15.1 <3.0.0"

dev_dependencies:
  lints: ^1.0.0
  test: ^1.16.0
dependencies:
  encrypt: ^5.0.1
